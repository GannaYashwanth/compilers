

void main(int c)
{
    int a[12];
    int i;
		i=0;
	//	a[i+1];
a[i+1] + a[i+2];
	//	i + i + i + a[i+1] + i + i + a[2] + a[i];
     //println(i, i+a[i]);
   // int i;
	/*for( i = 0 ; i < 5 ; i++ )
	{
		//arr[i] = scan();
	}
	for( i = 0 ; i < 5 ; i++ )
	{
		//sum += arr[i];
	}*/
	
//	print(sum);
}
